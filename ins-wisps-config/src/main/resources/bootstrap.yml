spring:
  application:
    name: ins-wisps
  config:
    import: nacos:ins-wisps.yml
  cloud:
    nacos:
      server-addr: localhost:8848
      config:
        namespace: d41a46f7-3b64-44c0-ab9f-b5766ca386d3
        group: wisps
        import-check:
          enabled: false # 如果项目暂时不需要nacos, 可以关掉启动检查, 项目就可以正常运行了